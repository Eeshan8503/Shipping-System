# CREATE TABLE CLIENT(MS VARCHAR(30) PRIMARY KEY,GST_NUM VARCHAR(20),LOCALITY VARCHAR(50),CITY VARCHAR(20),STATE VARCHAR(20), PINCODE VARCHAR(10), ACC_NUM VARCHAR(20));
# CREATE TABLE CONTAINER(CONTAINER_NUM VARCHAR(25) PRIMARY KEY,VESSEL VARCHAR(30), DESTINATION VARCHAR(30));
# CREATE TABLE SHIPPING_AGENT(INVOICE_NUM VARCHAR(20) PRIMARY KEY, MS VARCHAR(30), CONSTRAINT FOREIGN KEY (MS) REFERENCES CLIENT (MS), DATE DATE);
#CREATE TABLE CHARGES(AMOUNT INT,CHARGE_TYPE VARCHAR(30),CURRENCY VARCHAR(20),QUANTITY INT,RATE INT,INVOICE_NUM VARCHAR(40), CONTAINER_NUM VARCHAR(30),CONSTRAINT FOREIGN KEY (INVOICE_NUM) REFERENCES SHIPPING_AGENT(INVOICE_NUM),FOREIGN KEY (CONTAINER_NUM) REFERENCES CONTAINER(CONTAINER_NUM));